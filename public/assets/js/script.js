



// saved articles page
// Grab the saved articles as a json
// $.getJSON("/saved", function(data) {
//     for (var i = 0; i < data.length; i++) {
//       $("#saved-articles").append("<p data-id='" + data[i]._id + "'>" + data[i].title + "<br />" + data[i].link + "</p>");
//     }
//   });